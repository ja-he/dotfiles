#!/usr/bin/bash
set -euo pipefail

zettelkasten_path=${HOME}/zettelkasten
day=`date '+%Y-%m-%d'`
filename=$1
category=${2:-public}

path_full=${zettelkasten_path}/${category}/${day}
mkdir -p ${path_full}
nvim ${path_full}/${filename}
